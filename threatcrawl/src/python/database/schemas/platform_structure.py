# from database.common import DataType

"""Structure of the platform structure collections

This collection will contain information about the structure of a specific platform,
like it's login fields, navigational buttons, etc.

A schema must have the following properties:
    - collection: str with the name of the collection
    - properties: dict with structure of the data in the collection
"""

platform_structure_schema = {
    'properties': {
    },
    'collection': 'platform structures'
}
