"""Structure of the raw data collection

This collection will store all requests made, together with the received responses

A schema must have the following properties:
    - collection: str with the name of the collection
    - properties: dict with structure of the data in the collection
"""

raw_data_schema = {
    'properties': {
    },
    'collection': 'raw data'
}
